<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.QuizzPartner" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- Removes material button tint -->
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorAccent">@android:color/darker_gray</item>
        <item name="colorPrimaryDark">@android:color/white</item>
    </style>

    <style name="Theme.CustomSplashScreenTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_logo</item>
        <item name="windowSplashScreenAnimationDuration">3000</item>
        <item name="postSplashScreenTheme">@style/Theme.QuizzPartner</item>
    </style>

    <style name="Theme.QuizzPartner" parent="Base.Theme.QuizzPartner" />
</resources>